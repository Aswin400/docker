command for pull the image

1.docker pull image(name)
2.docker images -->(to see the docker information)
3.docker run images(name) ---> To run the image in terminal
4.docker ps (to see details for running image)
5. docker ps -a (to see the exit image containers)
6.docker  run -it python  -->( TO connect the image and docker to run images python commands)